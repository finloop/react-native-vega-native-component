*.iml
*.swp
.idea
.DS_Store
.vscode
.tmp

dist
build
node_modules
CMakeUserPresets.json
npm-debug.log
# Mesa libs pulled from the device for linking (see README GPU section)
device-libs

# Kepler UI-toolkit IDL copied from the SDK so the in-app build can import
# com.amazon.kepler.uitoolkit.view/.react (see README / memory). Regenerate with
# the cp from KeplerUI{Graphics,View,React}Interface package include dirs.
apmf

# generated by build-vega
CMakeUserPresets.json
buildinfo.json
dist
